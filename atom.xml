---
title : Atom Feed
---
<?xml version="1.0" encoding="UTF-8"?>
<feed version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:dc="http://purl.org/dc/elements/1.1/">
    <title>{{ site.name | xml_escape }}</title>
    <description>{% if site.description %}{{ site.description | xml_escape }}{% endif %}</description>
    <link href="{{ site.production_url }}/{{ site.atom_path }}" rel="self"/>
    <link>{{ site.production_url }}</link>
    <updated>{{ site.time | date_to_xmlschema }}</updated>
    <id>{{ site.production_url }}</id>
    <author>
        <name>{{ site.author.name }}</name>
        <email>{{ site.author.email }}</email>
    </author>
    {% for post in site.posts limit:site.feed_total %}
    <entry>
        <title>{{ post.title | xml_escape }}</title>
        {% for author in site.data.authors %}{% if post.author == author.name %}<author>{{ author.full_name | xml_escape }}</author>{% endif %}{% endfor %}
        <category>{{ post.category }}</category> 
        <description>{{ post.content | markdownify | xml_escape }}</description>
        <pubDate>{{ post.date | date_to_rfc822 }}</pubDate>
        <link>{{ site.url }}{{ post.url }}</link>
        <guid isPermaLink="true">{{ site.url }}{{ post.url }}</guid>
    </entry>
    {% endfor %}
</feed>